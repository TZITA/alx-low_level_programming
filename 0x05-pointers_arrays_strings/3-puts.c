#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts - puts strings to stdout
 * @str: string
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
	int i;
	int str[i];
	for(i = 0; i < strlen(str); i++)
	{
		putchar(str[i]);
	}
	putchar('\n');
