#include "main.h"
#include <stdio.h>

/**
 * _puts - puts strings to stdout
 * @str: string
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
	int str[i];
	for(i = 0; i < strlen(str); i++)
	{
		putchar(str[i]);
	}
	putchar('\n');
