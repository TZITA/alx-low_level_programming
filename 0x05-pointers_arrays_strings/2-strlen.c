#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - outputs the string length of a string
 * @s: string
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int l;

	l = sizeof(*s) / 8;
	return (l);
}
